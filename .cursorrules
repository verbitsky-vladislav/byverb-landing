# Адаптивность - четкие брейкпоинты

## 📱 Брейкпоинты для адаптации:

### **Очень маленькие телефоны** (< 360px)
- Очень маленькие смартфоны
- **Класс**: `xs:hidden` или `max-width: 359px`
- **CSS**: `@media (max-width: 359px)`

### **Базовые смартфоны** (360px - 479px)
- Стандартные смартфоны в портрете
- **Класс**: `xs:block sm:hidden` или `min-width: 360px max-width: 479px`
- **CSS**: `@media (min-width: 360px) and (max-width: 479px)`

### **Большие телефоны** (480px - 639px)
- Большие смартфоны, фаблеты
- **Класс**: `sm:block md:hidden` или `min-width: 480px max-width: 639px`
- **CSS**: `@media (min-width: 480px) and (max-width: 639px)`

### **Планшеты** (640px - 767px)
- Планшеты в портрете, маленькие планшеты
- **Класс**: `md:block lg:hidden` или `min-width: 640px max-width: 767px`
- **CSS**: `@media (min-width: 640px) and (max-width: 767px)`

### **Маленькие планшеты** (768px - 1023px)
- Планшеты в ландшафте, маленькие ноутбуки
- **Класс**: `lg:block xl:hidden` или `min-width: 768px max-width: 1023px`
- **CSS**: `@media (min-width: 768px) and (max-width: 1023px)`

### **Ноутбуки** (≥ 1024px)
- Стандартные ноутбуки, мониторы
- **Класс**: `xl:block` или `min-width: 1024px`
- **CSS**: `@media (min-width: 1024px)`

## 🎯 Логика адаптации:

```css
/* Очень маленькие телефоны */
@media (max-width: 359px) { ... }

/* Базовые смартфоны */
@media (min-width: 360px) and (max-width: 479px) { ... }

/* Большие телефоны */
@media (min-width: 480px) and (max-width: 639px) { ... }

/* Планшеты */
@media (min-width: 640px) and (max-width: 767px) { ... }

/* Маленькие планшеты */
@media (min-width: 768px) and (max-width: 1023px) { ... }

/* Ноутбуки и больше */
@media (min-width: 1024px) { ... }
```

## 🛠️ Tailwind классы:

- **Только очень маленькие телефоны**: `xs:hidden`
- **Только базовые смартфоны**: `xs:block sm:hidden`
- **Только большие телефоны**: `sm:block md:hidden`
- **Только планшеты**: `md:block lg:hidden`
- **Только маленькие планшеты**: `lg:block xl:hidden`
- **Только ноутбуки**: `xl:block`
- **Телефоны (все)**: `xl:hidden`
- **Планшеты + ноутбуки**: `md:block`

## 📋 Принципы адаптации:

1. **Очень маленькие телефоны**: Минимальная функциональность, большие кнопки
2. **Базовые смартфоны**: Стандартная мобильная версия
3. **Большие телефоны**: Улучшенная мобильная версия, больше контента
4. **Планшеты**: Промежуточная компоновка, средние размеры
5. **Маленькие планшеты**: Продвинутая планшетная версия
6. **Ноутбуки**: Полная функциональность, сложная компоновка

## 🎨 Примеры использования:

```jsx
{/* Только для очень маленьких телефонов */}
<div className="xs:hidden">Минимальная версия</div>

{/* Только для базовых смартфонов */}
<div className="xs:block sm:hidden">Стандартная мобильная версия</div>

{/* Только для больших телефонов */}
<div className="sm:block md:hidden">Улучшенная мобильная версия</div>

{/* Только для планшетов */}
<div className="md:block lg:hidden">Планшетная версия</div>

{/* Только для маленьких планшетов */}
<div className="lg:block xl:hidden">Продвинутая планшетная версия</div>

{/* Только для ноутбуков */}
<div className="xl:block">Десктопная версия</div>

{/* Адаптивные размеры */}
<div className="text-xs xs:text-sm sm:text-base md:text-lg lg:text-xl xl:text-2xl">
  Адаптивный текст
</div>
``` 